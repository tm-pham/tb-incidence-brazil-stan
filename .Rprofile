# .Rprofile: sourced at R startup from the project root.
# Phase 3 wires this up: activate renv, then source the project config so paths,
# the global seed, and mc.cores are available in every session.

# source("renv/activate.R")                       # enabled after renv::init()
# if (file.exists("code/00_config/config.R")) source("code/00_config/config.R")
