# _targets.R: end-to-end pipeline (targets package).
# Wired up in Phase 5 on synthetic data: prepare Stan data, fit the base model,
# run diagnostics, extract estimates. Stub for now.

# library(targets)
# list()
