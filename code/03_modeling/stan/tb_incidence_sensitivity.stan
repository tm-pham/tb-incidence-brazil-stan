// tb_incidence_sensitivity.stan
// Prior-sensitivity variant. Implemented after the base model works.
//
// Systematically varies the load-bearing priors (delta, mu, pi, the
// death-adjustment rho) across plausible ranges to report how much the incidence
// and fraction-treated estimates move. The honest test of how much conclusions
// rest on the priors rather than the data.
