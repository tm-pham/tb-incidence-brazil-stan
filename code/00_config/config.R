# config.R: project-wide path constants, global seed, and mc.cores.
# Implemented in Phase 3. Defines DATA_RAW, DATA_INTERIM, DATA_PROCESSED,
# DATA_SYNTHETIC, OUTPUTS and its subdirs via here::here(), plus GLOBAL_SEED and
# mc.cores. Sourced by .Rprofile.
